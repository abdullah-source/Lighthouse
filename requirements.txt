# Anthropic SDK — talks to Claude models.
# We use Claude Sonnet 4.6 for query generation, and Claude Haiku 4.5 for parsing.
anthropic>=0.40

# OpenAI SDK — talks to GPT models.
# We use GPT-5 as one of the two LLMs we probe on behalf of the brand.
openai>=1.50

# httpx — modern async HTTP client. Both SDKs above use it under the hood.
# We also call it directly in probe.py for the Perplexity (Sonar) retrieval
# probe, because the cited sources live in extra JSON fields the OpenAI SDK
# would drop.
httpx>=0.27

# python-dotenv — loads a .env file into environment variables at startup.
# Lets us keep API keys out of code and out of git.
python-dotenv>=1.0

# Postgres (Supabase) + pgvector — the v2 database and vector store.
# psycopg is the driver, psycopg_pool a shared connection pool, pgvector the
# vector type adapter. DATABASE_URL (in .env) points at Supabase.
psycopg[binary]>=3.1
psycopg_pool>=3.2
pgvector>=0.2

# FastAPI — the v1 backend. Async-native, reuses the probe/parse pipeline.
fastapi>=0.110

# Uvicorn — the ASGI server that runs FastAPI.
uvicorn[standard]>=0.29

# MCP — exposes Lighthouse's data as tools for the customer's own AI agents
# (Claude Desktop, Cursor). See mcp_server.py.
mcp>=1.0

# numpy — vector math for the retrieval-simulation feature (retrieval.py):
# cosine ranking of candidate pages against a query embedding.
numpy>=1.26
